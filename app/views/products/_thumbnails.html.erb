<% if product.images.size > 1 %>
  <ul id="product-thumbnails" class="thumbnails">
    <% product.images.each do |i| %>  
      <li><%= link_to(image_tag(cloudfront(i.attachment.url(:mini))), cloudfront(i.attachment.url(:product))) %></li>
    <% end %>
  </ul>
<% end %>
