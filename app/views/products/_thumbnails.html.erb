<% if product.images.size > 1 %>
  <ul id="product-thumbnails" class="thumbnails">
    <% product.images.each do |i| %>  
      <li><%= link_to(image_tag(i.cloudfront_url(:mini)), i.cloudfront_url(:product)) %></li>
    <% end %>
  </ul>
<% end %>
